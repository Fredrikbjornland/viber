import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme();

createMuiTheme({
    palette: {
        primary: { main: "#000000" },
        secondary: { main: "#141C3C" },
    },
});
export default theme;
